# 2026-08-19

## 今日目标

确认 Phase 2 的地图输出：3D 地图、2D/2.5D 地图和后续导航可用性。

## 实际完成

- 复跑 `bags/real_mid360/dynamic/mid360-loop-20260814-180225` 的 Lightning-LM 离线 SLAM。
- 成功生成并保存 3D 地图。
- 确认当前真机配置 `src/lightning_real/config/mid360_real.yaml` 中 `system.with_g2p5: false`，所以这次不会输出 `map.pgm` / `map.yaml`。

## 修改文件

- `CURRENT_STATUS.md`
- `CHANGELOG_AI.md`

## 关键命令 / 配置

- `source install/setup.bash`
- `src/lightning_real/scripts/run_offline_slam.sh bags/real_mid360/dynamic/mid360-loop-20260814-180225`
- `src/lightning_real/config/mid360_real.yaml`

## 测试及结果

- 离线 SLAM 结果：`exit_status=0`
- 保存路径：`runs/real_mid360/offline-mid360-loop-20260814-180225-20260819-122805/data/new_map/`
- 保存文件：`0.pcd`、`global.pcd`、`index.txt`

## 已解决问题

- 之前的 `liblightning.libs.so` 加载失败已通过修复 `install/lightning/lib/` 指向当前工作区的链接解决。

## 遗留问题

- 当前真机配置未启用 `g2p5`，还没有 Nav2 可直接用的 2D/2.5D 地图产物。

## 下一步

- 确认是否启用 `g2p5` 产出 ROS 兼容栅格地图。
- 继续检查离线地图定位阶段需要的地图路径和 frame 约定。
