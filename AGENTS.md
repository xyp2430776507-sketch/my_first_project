# AGENTS.md

本文件是新 AI 接手本仓库的固定工作流。除代码、命令、路径、topic、API 和必要技术术语外，`CURRENT_STATUS.md`、`CHANGELOG_AI.md`、`docs/`、`AGENTS.md` 下的项目交接与工作记录默认使用中文编写。

## 固定口令

### “开始工作”

收到后：
1. 先读取 `CURRENT_STATUS.md` 和 `docs/daily/` 最新一篇日志。
2. 简短恢复：当前状态、遗留问题、下一步。
3. 不要默认读取全部历史日志、CHANGELOG_AI.md、ARCHITECTURE.md 或全部源码。
4. 只有当前任务需要时，才读取相关架构文档、历史记录和源码。
5. 已经从文档获得的信息不要通过大范围源码搜索重复确认，除非信息过时或需要验证。
6.简短告诉我：当前状态、遗留问题、建议下一步。
7. 此阶段不要修改代码。

### “收尾”

收到后：

1. 检查本次实际代码/配置修改和测试结果。
2. 更新 `CURRENT_STATUS.md`：只保留当前有效状态、问题、下一步。
3. 更新 `CHANGELOG_AI.md`：记录重要代码/配置修改、原因和结果。
4. 创建或更新当天 `docs/daily/YYYY-MM-DD.md`，记录：
   - 今日目标
   - 实际完成
   - 修改文件
   - 关键命令/配置
   - 测试及结果
   - 已解决问题
   - 遗留问题
   - 下一步
5. 只记录可确认事实，不猜测、不虚构测试结果。
6. 收尾阶段不要再修改源码。
7. 最后简短给出建议的 git commit message。
8. 不执行 `git add` / `git commit` / `git push`。

### “记录这个问题”

把刚刚已经确认的问题整理到 `docs/problems/` 合适的主题文件。

记录：现象、原因、排查过程、解决方案、验证结果。

不要保存无价值的聊天过程；不确定内容标记为“待验证”。

### “记录这个结论”

把刚刚确认、以后有复用价值的知识整理到 `docs/notes/` 或现有相关文档。

优先更新已有主题文件，避免重复建文件。

### “记录这个操作”

把刚刚确认可复用的操作流程整理到 `docs/guides/`。

记录前提、步骤、关键命令、验证方法和注意事项。

## Token / 命令使用原则

目标：在保证正确性的前提下尽量减少 token 消耗。

1. 不要无目的扫描整个仓库、通读全部源码或读取全部历史日志。
2. “开始工作”默认只读：

   - `CURRENT_STATUS.md`
   - 最新一篇 `docs/daily/`
   - 当前任务直接相关的少量文件
3. `README.md`、`ARCHITECTURE.md`、`CHANGELOG_AI.md`、旧 daily 只在当前问题需要时读取。
4. 已经确认过的信息不要重复检查，除非代码或环境后来发生变化。
5. 能让我在终端执行的只读检查命令，优先直接给我命令，不要自己大量运行和分析。
6. 每次最多给 1～3 条必要命令，等我返回结果后再继续。
7. 命令尽量合并，避免多个等价检查。
8. 输出保持简短：

   - 结论
   - 要执行的命令
   - 成功判断
   - 下一步
9. 除非我说“详细解释”，否则不要重复解释已经讲过的 ROS2 / TF / SLAM 基础知识。
10. 不要重复粘贴完整源码；只指出相关文件、函数和必要代码片段。
11. 修改代码前只读取与当前修改直接相关的文件。
12. 搜索源码时先使用 `grep` / `rg` 精确定位，再读取目标片段，不要整文件无差别展开。
13. 长日志优先让我运行过滤命令，例如 `grep`、`head`、`tail`、`ros2 topic info`，只分析关键输出。
14. 如果一个检查我可以安全执行，就直接告诉我：

   - 在 PC 还是 RK 执行
   - 命令
   - 把哪部分输出发回来
15. 不为了节省 token 跳过必要的安全检查。

后续 Phase 1～6 均遵守这些规则。

## 通用规则

- 不依赖聊天历史，仓库文档是长期交接依据。
- 先检查再修改，尽量最小改动。
- 不重复记录相同信息。
- 未经明确确认，不删除文件、不执行危险命令、不修改系统/网络/ROS2 全局环境。
- 不执行 Git 操作，除非用户明确要求。
- 文档必须能让没有任何历史上下文的新 AI 快速接手。
- 代码具体差异由 Git 保存，文档重点记录“为什么改、结果如何、现在做到哪里”。
- 重要修改后更新 `CURRENT_STATUS.md`、`CHANGELOG_AI.md` 和当天 `docs/daily/YYYY-MM-DD.md`。
- 不允许虚构测试结果；只记录实际执行过的命令和确认过的结果。
- 触发“记录这个结论”或“记录这个操作”时，如缺少 `docs/notes/` 或 `docs/guides/`，可创建对应目录。
