# 2026-08-20

## 今日目标

- 完成 Phase 3 Localization / TF Integration 验证。
- 验证水平 `map` 坐标系是否可以接在 Lightning 原始 `lightning_map` 外部。
- 只把 Phase 4 记录为下一步计划，不标记为已完成。

## 实际完成

- 确认 Lightning-LM online localization 可以使用 Phase 1 生成的真实 MID360 3D 地图进行定位。
- 确认 Lightning 原生定位输出为 `map -> base_link`。
- 确认 bag / 底盘侧存在 `odom -> base_link`，直接并入 `/tf` 会形成 `base_link` 双父节点冲突。
- 将 Lightning 原生 `/tf` 通过 ROS 2 remap 隔离到 `/lightning_tf`。
- 新增并验证 `lightning_map_to_odom_tf.py` wrapper，输出 `lightning_map -> odom`。
- 完整单圈 bag 验证 `lightning_map -> odom -> base_link` 连续可用。
- 使用地面法向量验证固定 `map -> lightning_map` 旋转方向。
- 验证水平化后 `map -> base_link` 的 z 漂移明显降低。
- 新增集成定位 launch，将 Lightning localization、TF wrapper 和固定 `map -> lightning_map` static TF 放到同一个入口。
- 集成定位 launch 默认从已验证地图运行目录启动 Lightning，使配置里的相对 `./data/new_map/` 指向 Phase 1 输出。
- 使用同一个真实 MID360 loop bag 复验集成定位 launch，确认完整水平 TF 链连续可用。
- 修复 `lightning_map_to_odom_tf.py` 在 launch shutdown 时输出 traceback 的退出路径噪声。
- 补充 `xyp_mid360_navigation` 的 launch 相关运行依赖声明。

## 修改文件

- `src/lightning-lm/src/app/run_loc_online.cc`
- `src/xyp_mid360_navigation/scripts/lightning_map_to_odom_tf.py`
- `src/xyp_mid360_navigation/launch/lightning_localization_tf.launch.py`
- `src/xyp_mid360_navigation/CMakeLists.txt`
- `src/xyp_mid360_navigation/package.xml`
- `README.md`
- `ARCHITECTURE.md`
- `CURRENT_STATUS.md`
- `CHANGELOG_AI.md`
- `docs/decisions.md`
- `docs/daily/2026-08-20.md`
- `docs/guides/lightning_tf_integration.md`
- `docs/problems/phase3_tf_conflict.md`

## 关键命令 / 配置

Lightning localization with isolated TF:

```bash
ros2 run lightning run_loc_online   --config /home/kepler/lightning_lm_ws/src/lightning_real/config/mid360_real.yaml   --ros-args --remap /tf:=/lightning_tf
```

TF wrapper:

```bash
ros2 run xyp_mid360_navigation lightning_map_to_odom_tf.py
```

Horizontal static TF validated for `map -> lightning_map`:

```bash
ros2 run tf2_ros static_transform_publisher   --x 0 --y 0 --z 0   --qx 0.008708 --qy 0.042288 --qz 0.0 --qw 0.999068   --frame-id map --child-frame-id lightning_map
```

Integrated localization TF launch:

```bash
ros2 launch xyp_mid360_navigation lightning_localization_tf.launch.py
```

Default map run directory used by the launch:

```text
/home/kepler/lightning_lm_ws/runs/real_mid360/offline-mid360-loop-20260814-180225-20260819-122805
```

Validation bag:

```text
runs/real_mid360/phase3c-tf-check-20260819-175147
runs/real_mid360/phase3e-launch-tf-check-20260820-095321/tf_bag
```

## 测试及结果

- Phase 3A: Lightning native online localization passed.
- Phase 3B: full-bag localization stability passed; loop start/end distance was about `5 cm` and no meter-scale jump was observed.
- Phase 3C: full-bag TF validation passed.

```text
/lightning_tf map->base_link count: 44707
/tf lightning_map->odom count:     44707
/tf odom->base_link count:          7457
max receive gap:                    ~0.022 s
```

- Phase 3D: horizontalization direction passed.

```text
before z_range: ~1.215 m
after z_range:  ~0.145 m
after z_std:    ~0.024 m
samples:        44707
```

- Integrated launch static checks passed.

```text
python3 -m py_compile src/xyp_mid360_navigation/launch/lightning_localization_tf.launch.py src/xyp_mid360_navigation/scripts/lightning_map_to_odom_tf.py
colcon build --packages-select xyp_mid360_navigation
ros2 launch xyp_mid360_navigation lightning_localization_tf.launch.py --show-args
```

- Phase 3E integrated launch full-bag TF validation passed.

```text
/tf messages:          52622
/lightning_tf messages: 45088
/tf_static messages:       1

/lightning_tf map->base_link count: 45088
/tf lightning_map->odom count:     45088
/tf odom->base_link count:          7534
/tf_static map->lightning_map:      1

final chain map->base_link z_range span: ~0.134 m
final chain z_std:                       ~0.0289 m
matched odom dt max:                     ~0.0377 s
```

- After shutdown-path fix, short launch shutdown smoke no longer produced wrapper traceback. The remaining `exit code -2` lines came from the test harness sending SIGINT through `timeout`.

## 已解决问题

- `run_loc_online` 原先不能直接接受 ROS 2 remap 参数；已通过调整参数解析解决。
- Lightning `map -> base_link` 与底盘 `odom -> base_link` 的双父节点冲突已通过 `/lightning_tf` 隔离和 wrapper 解决。
- Lightning 原始地图约 5 度倾斜的问题已通过外部水平 `map` frame 验证解决方向。
- `map -> lightning_map` static TF 已固化进集成定位 launch。
- 集成定位 launch 已通过同包完整 bag TF 复验。

## 遗留问题

- 还没有生成或确认 Nav2 可使用的 2D/2.5D navigation map。
- Phase 4 尚未开始正式验收。

## 下一步

1. 进入 Phase 4：Nav2 配置、地图输入和速度链路检查。
