# AGENTS

- Every task starts by reading `CURRENT_STATUS.md` and the latest file in `docs/daily/`.
- Check before modifying.
- Keep changes as small as possible.
- Do not delete files or run dangerous commands unless explicitly confirmed.
- After important work, update `CURRENT_STATUS.md` and the daily log.

